#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>

#define MAX_SIZE 50

struct venda {
   		char* prod;
   		double price;
   		int quantity; 
   		char type;
   		char* cli;
   		int month;
   		int shop;
	};
